# HDR120 HEVC Bottleneck Map

Last updated: 2026-05-14.

## Current Best

- Best score: 110.00 from experiment 2122, kept because it restores source delivery with first-clean-complete-frame raw SkyLight warm-up and removes HEVC evaluator drops.
- Stable HEVC tile-record shape: experiment 2122 reports 192 encoded tile records / HDR records in the 1 s probe, with 2 independent half-width encoded lanes and zero drops.
- Stable HEVC logical-group shape: experiment 2122 reports 95 complete frame groups and 2 incomplete groups. Experiment 2126 tied the best score with 96 complete groups and 1 incomplete group, but it was discarded because ties are not keepable and still miss 120 complete logical frames.
- Current ProRes shape: experiment 2122 reaches 140 ProRes frames / 140 HDR records with zero drops.
- Contract: keep HEVC Main10, PQ HDR, target 3512x2290, 120 fps, low latency, and existing quality/bitrate policy. Do not trade quality or high refresh for score.

## Measured Pipeline

| Boundary | Representative measurement | Interpretation |
| --- | ---: | --- |
| HEVC source/tiling | 2063: 194 tile records, 194 HDR records, max tile count 2, encoded lane count 2 | Protocol-level tile records bypass the old single full-frame 101-102 output ceiling. |
| HEVC logical groups | 2063: 97 complete frame groups, 0 incomplete groups | Column partitioning improves logical-frame throughput, but it remains below 120 groups/s. |
| HEVC VT submission | 2063: 103 staged/submitted half-width tile frames | The HEVC tile path reaches enough tile records for the score cap, but each logical frame needs 2 tile submissions. |
| HEVC per-lane VT output | 2068 diagnostic: lane 0 and lane 1 each submitted 103 staged frames and completed 97 output callbacks | The 97-group ceiling is symmetric across both lanes, not a single slow-lane imbalance. |
| HEVC callback/stability | 2063: 70.483 ms avg callback latency, 0 drops | Stability is currently fixed for the tile-record path; do not reopen naive tile emission or forwarding capacity churn. |
| HEVC Metal/VT tail latency | 2072 diagnostic: Metal stage avg/P95/P99 4.681/14.489/18.959 ms; VT encode call avg/P95/P99 10.179/21.065/21.363 ms; output callback avg/P95/P99 71.147/84.074/84.150 ms | Averages hide the first visible tail stall. VT encode-call P95 is already above the 8.33 ms frame budget, so source replay/mailbox work is below priority. |
| HEVC independent tile freshness | 2073 diagnostic: 193 tile records, 96 strict complete groups, 192 freshness-composite updates, 192 HDR freshness-composite updates | The stream already has over-120 independent tile updates; the remaining question is whether the product accepts partial freshness with explicit skew bounds instead of strict same-group frame completion. |
| HEVC skew-bounded freshness | 2074 diagnostic: 192 freshness updates, 65 within 8.333 ms skew, 116 within 16.667 ms skew, max skew 41.361 ms | Partial freshness is not automatically perceptually valid. The receiver needs a tile-age/skew policy before 192 updates can be treated as product-level 120 fps. |
| HEVC skew distribution | 2075 diagnostic: 192 freshness updates, 61 within 8.333 ms, 117 within 16.667 ms, 158 within 25.0 ms, 184 within 33.333 ms, 8 over 33.333 ms, max skew 42.138 ms; older tile 1 in 159 updates versus older tile 0 in 1 | Skew is not balanced random drift. Tile 1 is almost always stale, so the next useful topology work should explain lane-1 tail age or define a bounded-staleness receiver contract that can hold/drop over-skew updates. |
| HEVC enqueue-order asymmetry | 2076 diagnostic: reversing tile enqueue order kept 192 freshness updates but flipped stale identity to older tile 0 in 156 updates versus older tile 1 in 4 | The stale-tile asymmetry follows processing/output order, not fixed spatial workload. Reverse order is not keepable because strict groups stayed 96 and HEVC still had 7 drops, but it narrows the root cause to ordering-induced freshness skew. |
| HEVC paired sender emission | 2077 diagnostic: sender-side paired group output improved freshness coherence to 121/187 within one frame, 182/187 within two frames, and 0 over four frames, but HEVC dropped to 94 complete groups with 16 drop events and 6 dropped forwarded frames | Sender-side micro-barriers can hide skew but reintroduce tail/drop amplification. Temporal arbitration should move to the receiver/compositor, not the encoder hot path. |
| HEVC per-lane admission | 2078 diagnostic: per-lane serial queues reduced encode queue wait to near zero, but HEVC still had 95 complete groups, 4 drops, and only 115/191 freshness updates within two frames | Lane admission serialization is not the freshness bottleneck. The remaining issue is downstream VT/output cadence plus receiver temporal semantics. |
| HEVC receiver arbitration | 2080 diagnostic: raw latest composite had 193 updates but only 114 within two frames; simple receiver arbitration presented 2 updates at one-frame budget, 64 at two-frame, 190 at three-frame, and 193 at four-frame, with longest same-tile arrival run 3 | A naive latest-pending bounded receiver model does not satisfy a two-frame product contract. It only exceeds 120 updates around a three-frame skew budget, so receiver work needs phase-aware/cadence-normalizing arbitration rather than a simple freshness rename. |
| HEVC phase-normalized receiver model | 2081 diagnostic: per-tile pending queues presented 130 two-frame updates and 178 three-frame updates, but the probe incurred 10 HEVC drop events and max two-frame queue depth 31 | The stream has enough two-frame-coherent opportunities if intermediate pending tile updates are preserved. Heavy queue arbitration in the capture probe is not keepable; the next receiver work should move cadence analysis off the hot path and explicitly cap queue depth. |
| HEVC offline phase model | 2082 diagnostic: compact trace plus post-capture FIFO-front phase arbitration produced only 32 two-frame updates and 38 three-frame updates, with max two-frame queue depth 81 and 3 HEVC drop events | Moving analysis offline is not enough if the model consumes queue fronts blindly. Receiver timebase reconstruction likely needs direct phase-distribution measurement or nearest-in-window pairing rather than FIFO-front pairing. |
| HEVC fixed-trace phase replay | 2140 diagnostic: fixed-buffer post-capture replay of the 2081 phase queue produced 8 two-frame updates and 158 three-frame updates with 0 drops and no trace overflow | The 2081 two-frame recovery does not survive as a low-overhead source-time replay contract. The official probe evidence still supports a three-frame bounded presentation model, not a two-frame product contract. |
| HEVC source-time nearest pairing | 2083 diagnostic: nearest matching tile 0 and tile 1 source-display-time sequences produced 95 pairs for one/two/three-frame budgets, equal to strict complete groups, while HEVC had 5 drop events | Source-display-time nearest matching only rediscovers strict groups. The useful phase signal is likely in receiver arrival/callback-drain timing or presentation scheduling, not just source timestamps. |
| HEVC arrival-time nearest pairing | 2084 diagnostic: nearest matching tile 0 and tile 1 probe arrival times produced 36 one-frame pairs and 95 two/three-frame pairs, equal to strict complete groups at two frames, while HEVC had 8 drop events | Probe arrival-time nearest matching also rediscovers strict groups. The 2081 recovery is a display-state update-policy effect after priming, not a hidden extra pair-count in source or arrival timestamps. |
| HEVC bounded partial presentation | 2085 diagnostic: depth-4 and depth-8 newest-pending partial presentation produced only 25 two-frame updates, while dropping 152-160 stale entries and causing 9 HEVC drop events | Simple bounded newest-pending stateful presentation is too aggressive and loses useful history. The remaining receiver path is not a small tweak to pending depth; it needs either exact 2081 semantics with lower overhead or a real client-side presentation contract. |
| HEVC tile contract readiness | 2139 diagnostic: runtime probe counted 159 contract-ready tile updates and 0 invalid tile records after tile-index priming, while strict complete groups stayed at 80 in the sampled run | Tile records already carry enough metadata for more-than-120 substream updates, but the product/evaluator still needs an actual presentation contract. More probe counters alone do not solve strict-group completion. |
| HEVC per-tile update metadata | 2145 diagnostic: encoded tile packets advertised `tileCount=1` with `encodedLaneCount=2` and tile-region metadata, producing 194 tile records, `MAX_TILE_COUNT=1`, and 0 drops, but complete groups stayed 97 | Tile-count-only metadata does not define an independent presentation group because the receiver/probe still groups by `frame_group_id` or source sequence. A per-tile update contract also needs distinct group IDs or receiver grouping semantics. |
| HEVC per-tile group identity | 2146 diagnostic: encoded tile packets used `tileCount=1` plus per-tile `frameGroupID`, producing 194 tile records, 194 complete groups, 0 incomplete groups, and 0 drops | This proves the 120 complete-group target is reachable by explicit presentation-contract semantics without changing encoder throughput. The remaining work is product/client validation and latency scoring, not more strict same-source-frame grouping. |
| HEVC fused 420 conversion | 2086 diagnostic: combined no-scale/no-cursor BGRA-to-420 Metal kernel produced 190 tile records, 94 complete groups, 2 incomplete groups, 7 drop events, and 100 VT submissions | Fusing luma/chroma conversion dispatches does not improve the HEVC ceiling and destabilizes tile cadence. The current bottleneck is not solved by simply reducing Metal pass count inside the same staged BGRA topology. |
| HEVC VT internal BGRA input | 2087 diagnostic: staged BGRA input with Main10/HDR session properties produced 176 tile records, 88 complete groups, 11 drop events, 98 VT submissions, and 118.450 ms average callback latency | Moving BGRA-to-HEVC conversion into VT is worse than explicit staged 420v10. Keep explicit 420v10 staged input unless a different ownership/pacing model proves otherwise. |
| HEVC tile PTS phase staggering | 2088 diagnostic: lane-index half-frame PTS staggering produced 192 tile records, 95 complete groups, 2 incomplete groups, 11 drop events, and 101 VT submissions | Separating tile-lane presentation timestamp phases does not improve VT/output cadence and worsens stability. The ceiling is not caused by both lanes sharing identical PTS cadence slots. |
| HEVC callback augmentation deferral | 2089 diagnostic: deferring HDR metadata augmentation from the VT callback entry to `outputQueue` produced 193 tile records, 96 complete groups, 1 incomplete group, 5 drop events, and 102 VT submissions | Callback entry work placement is not the primary drain bottleneck. Moving HEVC HDR augmentation off the callback thread does not improve callback latency or strict groups. |
| HEVC explicit encode duration | 2090 diagnostic: passing `1 / targetFrameRate` as each VT encode-frame duration produced 192 tile records, 94 complete groups, 4 incomplete groups, 9 drop events, and 100 VT submissions | Per-frame duration hints destabilize HEVC tile cadence. The session-level `ExpectedDuration` hint is better than concrete per-frame duration for this path. |
| HEVC shared Metal command queue | 2091 diagnostic: sharing one Metal command queue across tile lanes produced 193 tile records, 96 complete groups, 1 incomplete group, 9 drop events, and 102 VT submissions | Independent Metal command queues are not the root cause of the VT drain ceiling. Shared queue ownership increases drop pressure without improving callback latency. |
| HEVC shared VT output queue | 2092 diagnostic: sharing one serial output queue across tile-lane VT processors produced 193 tile records, 96 complete groups, 1 incomplete group, 6 drop events, and 102 VT submissions | Per-lane output-handler delivery concurrency is not the root cause of the VT drain ceiling. Shared output queue ownership does not improve callback latency or strict groups. |
| HEVC session prepare skip | 2093 diagnostic: skipping `VTCompressionSessionPrepareToEncodeFrames` for HEVC PQ tile streams produced 193 tile records, 96 complete groups, 1 incomplete group, 6 drop events, and 102 VT submissions | Explicit session preparation is not causing the VT drain ceiling. Lazy-starting the tile sessions leaves callback latency and strict groups unchanged. |
| HEVC shared tile VT session | 2094 diagnostic: one shared half-width HEVC VT session for both tile regions produced 193 tile records, 96 complete groups, 1 incomplete group, 7 drop events, 199 VT submissions, and 35.938 ms average callback latency | VT session topology materially affects callback latency, but one-session tile over-admission does not improve logical completion. Session contention is real, yet strict groups remain contract-bound or admission-bound. |
| HEVC shared tile VT session cadence | 2095 diagnostic: shared tile VT session with tile-record frame-rate hint produced 195 tile records, 97 complete groups, 1 incomplete group, 7 drops, 201 VT submissions, and 35.956 ms average callback latency | Correcting shared-session cadence slightly improves records/groups while keeping lower callback latency, but stability still fails. The remaining shared-session problem is admission/backpressure, not cadence hint alone. |
| HEVC shared tile VT session alternating order | 2138 diagnostic: shared tile VT session with source-sequence alternating tile submission produced 157 tile records, 78 complete groups, 1 incomplete group, 0 drops, 158 VT submissions, and 16.606 ms average callback latency | Alternating shared-session submit order lowers average callback latency by reducing delivered tile work, not by improving throughput. Ordered-submit balancing is not a safe way to rescue the shared-session branch. |
| HEVC shared tile VT session max realtime cap | 2141 diagnostic: shared tile VT session with `MaximumRealTimeFrameRate` constrained to the 240 fps tile-record cadence produced 172 tile records, 86 complete groups, 0 drops, 174 VT submissions, and 21.883 ms average callback latency | Constraining VT maximum realtime cadence reduces measured latency by reducing useful tile output. Lower callback latency is not sufficient; shared-session admission control cannot be replaced by a tighter VT max realtime hint. |
| HEVC shared tile VT session deferred secondary submit | 2144 diagnostic: shared tile VT session with the secondary tile submitted on a later serial queue turn produced 194 tile records, 97 complete groups, 0 incomplete groups, 0 drops, 198 VT submissions, and 35.854 ms average callback latency | One-turn non-blocking deferral is safe but does not beat the 2134 shared-session 98-group shape. Same-call-stack burst admission is not the remaining shared-session limiter by itself. |
| HEVC shared tile VT session hard in-flight cap | 2096 diagnostic: shared tile VT session with tile-record cadence and `maxInflightStagingSlots=12` produced 175 tile records, 83 complete groups, 9 incomplete groups, 0 drops, 14 failure events, 146 VT submissions, and 34.933 ms average callback latency | A raw staging-slot cap restores pressure by failing admission, not by stabilizing the stream. Shared-session admission control must be non-error pacing/deferral rather than `stagingSlotUnavailable` on the hot path. |
| HEVC shared tile VT session soft pending pacing | 2097 diagnostic: shared tile VT session with tile-record cadence and pending-submission cap 18 failed the HEVC runtime probe with a large stability penalty and repeated capture-queue saturation/restarts | Synchronous waiting in the capture/encode hot path is not valid admission control. Pacing must be non-blocking and decoupled from source capture, or the receiver contract must consume tile substreams without strict frame completion. |
| HEVC shared tile VT session same-PTS grouping | 2098 diagnostic: shared tile VT session with both tiles from each source frame using the same logical-frame PTS produced 0 HEVC frames/submissions and also destabilized ProRes probing | VT does not accept identical same-session tile timestamps as a logical-pair grouping primitive. Shared-session timestamp grouping is closed. |
| HEVC shared tile VT session non-blocking staged drain | 2099 diagnostic: shared tile VT session with tile-record cadence and pending staged CVPixelBuffer drain produced 190 tile records, 95 complete groups, 0 incomplete groups, 0 drops, 196 VT submissions, 36.545 ms average callback latency, and 5.316 ms average VT encode call; ProRes proxy produced 0 frames | Non-blocking staged drain preserves the lower callback-latency regime but does not improve strict groups and breaks the ProRes suite. A generic encoder-level staged-submission queue is not a safe admission-control primitive for the shared pipeline. |
| HEVC shared tile VT session source-window cap | 2100 diagnostic: shared tile VT session with tile-record cadence and HEVC-tile SkyLight pending window reduced to 12 failed the HEVC runtime probe with a large stability penalty; ProRes proxy also produced 0 frames in the captured official metric output | Source-level pending-window caps are not safe shared-session admission control. They destabilize ingress before useful HEVC tile counters are available and do not isolate ProRes enough to preserve the suite contract. |
| HEVC shared tile VT session source-window expansion | 2101 diagnostic: shared tile VT session with tile-record cadence and HEVC-tile SkyLight pending window raised to 24 also failed the HEVC runtime probe with a large stability penalty; ProRes proxy also produced 0 frames in the captured official metric output | Source-level pending-window tuning is closed in both directions. Wider ingress buffering does not absorb shared-session bursts; it destabilizes the suite like the narrower window. |
| HEVC warm-up callback-side stop | 2127 diagnostic: scheduling `SLDisplayStreamStop` from the first clean complete warm-up callback produced 193 tile records, 94 complete groups, 5 incomplete groups, 1 drop event, 100 VT submissions, and warm-up callback count 2 / complete frame count 1 | Moving raw warm-up stop ownership into the callback-triggered async path does not remove queued callback perturbation and worsens group coherence. The 2126 one-callback improvement came from post-clean callback suppression, not earlier stop ownership. |
| HEVC first-clean warm-up fast-exit | 2128 diagnostic: returning from the first clean complete warm-up callback before reduced-dirty-rect accounting produced 193 tile records, 96 complete groups, 1 incomplete group, 30 HEVC drop events, and warm-up callback count 3 / complete frame count 2 | Pre-signal/fast-exit shortcutting destabilizes the source state. Do not shortcut the first clean callback before normal benchmark bookkeeping; it increases warm-up callbacks and severe HEVC drop pressure. |
| HEVC first-clean warm-up queue depth | 2129 diagnostic: lowering only the warm-up benchmark queue depth to 1 produced 194 tile records, 97 complete groups, 0 incomplete groups, 103 VT submissions, but 31 HEVC drop events | Warm-up queue-depth reduction restores the older 97/0 group shape but collapses the no-drop contract. It is a tradeoff knob, not a safe source-unblock cleanup. |
| HEVC warm-up queue plus suppression | 2130 diagnostic: combining warm-up queue depth 1 with post-clean callback suppression reduced warm-up callbacks to 1 / 1 complete frame, but produced only 192 tile records, 95 complete groups, 2 incomplete groups, and 25 HEVC drop events | Post-clean callback suppression does not control the drop pressure caused by lower warm-up queue depth, and it loses the 2129 group-shape benefit. This closes the simple combination of those two warm-up cleanup levers. |
| HEVC per-lane queues plus warm-up suppression | 2131 diagnostic: combining per-lane tile serial queues with post-clean warm-up suppression reduced encode queue wait to 0.001 ms average / 0.009 ms max, but produced 190 tile records, 94 complete groups, 2 incomplete groups, and 2 HEVC drop events | Lane enqueue wait is not the group-coherence bottleneck even when paired with the one-callback warm-up cleanup. The current issue remains downstream VT/output cadence or presentation contract. |
| HEVC VT pending-depth polling | 2132 diagnostic: sampling `NumberOfPendingFrames` every 8 encoded submissions produced 191 tile records, 94 complete groups, 3 incomplete groups, 4 drop events, 100 VT submissions, and 70.560 ms average callback latency | VT pending-depth property reads are too perturbative for the official hot path and do not recover group completion. Do not use VT pending polling as the admission/backpressure signal in this pipeline. |
| HEVC tile source release after Metal | 2133 diagnostic: releasing HEVC tiled source frames immediately after Metal staging produced 192 tile records, 95 complete groups, 2 incomplete groups, 0 drops, 101 VT submissions, and 70.675 ms average callback latency | Holding the original source IOSurface until VT submit is not the strict-group bottleneck. Post-Metal release is HEVC-safe but does not improve groups and reduces ProRes progression. |
| ProRes source cadence | 2063: 135 source frames, 134 records, about 123.4 fps | ProRes frame count is no longer the limiting stage when catch-up replay is isolated from HEVC. |
| ProRes VT encode | 2063: 135 submissions, 1.482 ms avg VT encode call, 0 drops | ProRes encoder remains fast enough after source cadence recovery. |
| Lumen forwarding ingress | 2058/2061/2062/2063: 0 queued, 0 dropped | The Lumen bridge is not dropping the current kept tile/prores paths. |
| Lumen tile-stream bridge queue | 2142 diagnostic: selecting q2 for encoded tile stream topology before the 120fps q1 fallback produced 180 tile records, 90 complete groups, 0 drops, and 41.421 ms average callback latency | Extra bridge queue slack does not recover strict tile-group cadence. The q2-before-120fps bridge seam is closed as a standalone fix. |

## Closed Or Low-Priority Axes

- Do not chase raw source availability unless source count, pending rejected count, or forwarding drops become nonzero in the same valid metric window.
- Do not repeat simple VT knobs: temporal compression disable, unretained command buffers, per-frame or batched `VTCompressionSessionCompleteFrames`, and diagnostic property deferral did not beat best.
- Do not repeat simple `submissionQueue` removal by calling VT directly from the Metal completion callback. Prior inline submit shifted blocking into the Metal-stage bucket without increasing useful submissions or outputs.
- Do not repeat naive temporal or spatial HEVC lane splitting under the current client/probe contract. It worsened callback latency or produced incomplete/dropped logical groups.
- Do not repeat independent HEVC tile emission without protocol-aware queue semantics. Experiment 2057 hit 120 HEVC HDR tile records but collapsed with 62 drops; 2058 fixed that by scaling forwarding capacity to the negotiated tile-record multiplier.
- Do not repeat ProRes replay-timer queue isolation as a standalone fix. Experiment 2059 kept the score capped at 110.00 but ProRes regressed to 106 frames and about 95.63 fps source cadence.
- Do not repeat shared replay catch-up that changes HEVC single-replay timestamp semantics. Experiment 2060 recovered ProRes but introduced 21 HEVC drop events. Experiment 2061 shows the keepable shape is ProRes-only batch replay with the original HEVC single-replay path preserved.
- Do not treat encoded tile-record count alone as proof of logical-frame completion. Experiment 2062 shows 178 stable tile records but only 89 complete logical tile groups.
- Do not revert to full-width half-height tile bands unless a later experiment proves columns break client decoding. Experiment 2063 raised complete logical groups from 89 to 97 with zero drops.
- Do not add a third HEVC tile lane as a direct throughput fix. Experiment 2064 raised tile records to 272 but regressed complete logical groups to 90 and introduced 1 incomplete group.
- Do not rotate 2-column tile lane processing order. Experiment 2065 regressed complete groups to 94, introduced 3 incomplete groups, and increased Metal/VT jitter.
- Do not round the 2-column split to a 64-pixel CTU boundary as a standalone fix. Experiment 2066 regressed complete groups to 94 and introduced 4 HEVC drop events.
- Do not move the existing 2-column lane work onto per-lane serial queues as a standalone fix. Experiment 2067 removed almost all encode queue wait but still produced 194 tile records, 97 complete groups, 0 drops, and 103 VT submissions, so source-callback lane enqueue serialization is not the first remaining bottleneck.
- Do not assume one tile lane is uniquely lagging. Experiment 2068 showed lane 0 and lane 1 both processed/submitted 103 tile frames and completed 97 callbacks. The detailed diagnostic path itself caused 1 HEVC drop event, so keep it as discard-only evidence rather than baseline instrumentation.
- Do not replace HEVC forwarding HDR detection with a format-description-only fast path as a standalone fix. Experiment 2069 improved ProRes but regressed HEVC to 95 complete groups, 2 incomplete groups, and 5 drop events, so per-frame HEVC HDR payload validation is not the current HEVC tile ceiling.
- Do not enable HEVC tile replay catch-up as a standalone source-cadence fix. Experiment 2070 regressed to 95 complete groups, 2 incomplete groups, and 21 drop events while VT submissions stayed at 101, so missing timer replay frames are not the first bottleneck.
- Do not preserve fresh mailbox frames over replay frames as a standalone HEVC tile-stream stability fix. Experiment 2071 reduced drop severity but regressed to 94 complete groups, 3 incomplete groups, and 100 VT submissions, so replay overflow filtering does not create more logical frames.
- Do not keep hot-path percentile arrays as baseline instrumentation. Experiment 2072 exposed useful tail evidence but scored 92.00 with 9 HEVC drop events, so use the data to guide topology changes and revert the instrumentation after the diagnostic run.
- Do not assume 120 strict complete groups is the only viable client contract. Experiment 2073 showed 192 independent tile freshness updates while strict groups stayed at 96; the viable topology may be a skew-bounded tile freshness stream rather than synchronous frame-group assembly.
- Do not equate all independent freshness updates with valid 120 fps. Experiment 2074 showed only 65/192 updates inside one 120 Hz frame of skew and 116/192 inside two frames, so the contract needs explicit tile-age handling.
- Do not treat `tileCount=1` alone as the per-tile presentation contract. Experiment 2145 left `frameGroupID=0`, so group keys still fell back to source sequence and complete groups stayed 97 despite `MAX_TILE_COUNT=1`.
- Do not keep per-tile group identity as a silent metadata relabel. Experiment 2146 reached 194 complete groups with 0 drops, proving the contract can satisfy the probe, but it still needs an explicit receiver/client compositor contract and latency story before it is product-safe.
- Do not treat skew-bounded freshness as a balanced stochastic problem. Experiment 2075 showed older tile 1 in 159/192 composite updates, older tile 0 in only 1/192, and 8 updates over four frames of skew. The next receiver or topology experiment must account for lane-specific staleness, not only global update count.
- Do not chase fixed right-tile workload as the primary explanation for freshness skew. Experiment 2076 reversed enqueue order and flipped staleness to older tile 0 in 156/192 updates, so the asymmetry follows lane processing/output order.
- Do not buffer complete tile groups on the sender hot path as the primary coherence fix. Experiment 2077 improved skew buckets but regressed HEVC to 94 complete groups with 16 drop events and 6 dropped forwarded frames.
- Do not retry per-lane serial queues as a freshness fix. Experiment 2078 measured the missing skew data: encode queue wait fell to near zero, but two-frame bounded freshness regressed to 115/191 and strict groups regressed to 95 with drops.
- Do not treat simple latest-pending receiver arbitration as solved. Experiment 2080 needed a three-frame skew budget to present over 120 tile updates, while two-frame arbitration only presented 64 updates and the probe still introduced 1 HEVC drop event.
- Do not run heavy per-tile receiver queue arbitration on the capture/probe hot path. Experiment 2081 recovered 130 two-frame updates but introduced 10 HEVC drop events and max queue depth 31.
- Do not use FIFO-front phase pairing as the receiver reconstruction model. Experiment 2082 moved it offline but still under-presented, built an 81-deep two-frame queue, and introduced 3 HEVC drop events.
- Do not retry offline 2081-style source-time phase replay as the receiver reconstruction model. Experiment 2140 kept the probe stable but recovered only 8 two-frame updates while three-frame updates reached 158.
- Do not use source-display-time nearest matching as the phase reconstruction model. Experiment 2083 produced exactly the strict-group pair count and still caused 5 HEVC drop events.
- Do not use probe arrival-time nearest matching as the phase reconstruction model. Experiment 2084 produced exactly the strict-group pair count at two/three-frame budgets and caused 8 HEVC drop events.
- Do not use bounded newest-pending partial presentation as the receiver model. Experiment 2085 produced only 25 updates and dropped most pending history while still causing 9 HEVC drop events.
- Do not stop at probe-only tile-contract counters. Experiment 2139 showed 159 contract-ready tile updates and 0 invalid tile metadata records, but strict complete groups were still far below target and the score only tied.
- Do not use a fused unscaled BGRA-to-420 Metal kernel as a standalone fix. Experiment 2086 reduced the conversion to one compute path but regressed HEVC to 94 complete groups with 7 drop events and 100 VT submissions.
- Do not use staged BGRA input with HEVC Main10/HDR properties as a standalone fix. Experiment 2087 moved conversion into VT and regressed to 88 complete groups, 11 drops, and 118.450 ms average callback latency.
- Do not use lane-index PTS phase staggering as a standalone VT cadence fix. Experiment 2088 kept tile records at 192 but still produced only 95 complete groups with 11 drop events, so identical lane PTS cadence slots are not the bottleneck.
- Do not defer only HEVC HDR static metadata augmentation out of the VT callback as a standalone callback-drain fix. Experiment 2089 still produced only 96 complete groups with 5 drop events and unchanged callback latency.
- Do not pass explicit per-frame VT encode duration as a standalone cadence fix. Experiment 2090 regressed to 94 complete groups with 9 drops and 4 incomplete groups; keep `.invalid` duration with session-level `ExpectedDuration`.
- Do not share a single Metal command queue across HEVC tile lanes as a standalone completion-ownership fix. Experiment 2091 still produced only 96 complete groups with 9 drops and unchanged callback latency.
- Do not share a single VT output queue across HEVC tile-lane processors as a standalone output-drain fix. Experiment 2092 still produced only 96 complete groups with 6 drops and unchanged callback latency.
- Do not skip `VTCompressionSessionPrepareToEncodeFrames` for HEVC tile streams as a standalone lifecycle fix. Experiment 2093 still produced only 96 complete groups with 6 drops and unchanged callback latency.
- Do not keep a single shared HEVC tile VT session as-is. Experiment 2094 halved callback latency but still produced only 96 complete groups, 1 incomplete group, and 7 drops while over-admitting 199 VT submissions.
- Do not treat tile-record cadence hints as sufficient for shared-session topology. Experiment 2095 improved to 195 records and 97 complete groups at lower callback latency, but still had 7 drops and 1 incomplete group.
- Do not alternate shared-session tile submit order as an ordered-output balancing trick. Experiment 2138 reduced average callback latency to 16.606 ms but collapsed HEVC to 157 tile records and 78 complete groups, so the apparent latency win came from delivering less useful work.
- Do not defer only the secondary tile by one serial queue turn as shared-session admission shaping. Experiment 2144 kept 0 drops and the lower callback regime, but only reached 194 records and 97 complete groups, below the 2134 shared-session 98-group shape.
- Do not use a hard `maxInflightStagingSlots` cap as shared-session admission control. Experiment 2096 removed drops but regressed to 83 complete groups, 9 incomplete groups, and 14 failure events.
- Do not block `stageAndEncode` waiting for pending VT callbacks as shared-session admission control. Experiment 2097 failed the HEVC runtime probe and triggered capture-queue saturation/restarts.
- Do not give both tiles in one shared VT session the same presentation timestamp as a grouping trick. Experiment 2098 produced zero HEVC output.
- Do not add a generic non-blocking staged CVPixelBuffer pending/drain queue inside the encoder as shared-session admission control. Experiment 2099 preserved the lower callback-latency regime but regressed strict groups to 95 and broke ProRes proxy output entirely.
- Do not reduce the HEVC tiled SkyLight pending-frame window as shared-session admission control. Experiment 2100 failed the HEVC runtime probe and did not preserve the ProRes suite.
- Do not increase the HEVC tiled SkyLight pending-frame window as shared-session admission control. Experiment 2101 failed the HEVC runtime probe and did not preserve the ProRes suite.
- Do not set the shared HEVC tile session's `MaxFrameDelayCount` to 0 as admission control. Experiment 2102 failed both runtime probes before useful HEVC/ProRes counters were available.
- Do not add drop-source classification directly to the official runtime probe as hot-path instrumentation. Experiment 2103 made the official probe produce zero HEVC and ProRes frames, so drop attribution must come from already-valid logs or a separate diagnostic path.
- Do not treat zero-frame `STATUS=ok` runtime-probe runs as throughput evidence. Experiment 2104 showed the clean measurement path can fail before any source frame reaches VT; fix or isolate startup/source delivery before interpreting encoder numbers.
- Do not rely on a probe-level stop/start retry to recover zero-frame startup. Experiment 2105 retried capture once, reported `STARTUP_RETRY_COUNT=1`, and still produced 0 HEVC/ProRes frames and 0 VT submissions.
- Do not chase Lumen core-forwarding as the first explanation for the current no-frame startup state. Experiment 2106 showed the HEVC capture session is running with 0 emitted frames, 0 processing failures, no last error, and 0 VT submissions.
- Do not chase source-to-processor handoff or VT for the current no-frame startup state before fixing SkyLight callback delivery. Experiment 2107 showed the source runtime timer ticks about 1200 times while `SKYLIGHT_SOURCE_CALLBACK_COUNT=0` for both HEVC and ProRes.
- Do not use private direct IOSurface fallback as the current measurement recovery path. Experiment 2108 forced raw SkyLight off in the probe and both HEVC/ProRes failed with a nonzero private-direct warmup status.
- Do not retry raw SkyLight source/destination rect or preserveAspectRatio property additions as the startup recovery path. Experiment 2109 added those properties in the probe and still produced 0 HEVC/ProRes frames and 0 VT submissions.
- Do not use nil raw SkyLight stream properties as the startup recovery path. Experiment 2110 made the raw stream create call receive nil properties and the official metric still failed with HEVC runtime failure and ProRes 0 frames/0 submissions.
- Do not continue simple raw SkyLight property-profile guessing as the primary startup branch. Experiment 2111 copied the existing private probe's rectless-minimal profile and still failed with HEVC runtime failure plus ProRes 0 frames/0 submissions.
- Do not repeat raw SkyLight callback-owner retention changes. Experiment 2112 made the block capture the session owner strongly and still produced 0 HEVC/ProRes frames and 0 VT submissions.
- Do not keep or repeat the generic public CGDisplayStream encoded-source wrapper as a recovery path. Experiment 2113 forced it only in the probe and both codec probes failed before usable frame diagnostics.
- Do not swap raw stream startup from `SLDisplayStreamStart` to public `CGDisplayStreamStart`. Experiment 2114 kept HEVC at 0 frames/0 submissions and made ProRes fail the runtime probe.
- Do not repeat raw SkyLight create/start availability or status diagnostics. Experiment 2115 showed the symbols exist, creation returns a stream, and `SLDisplayStreamStart` returns success, while HEVC still receives 0 callbacks/frames/submissions.
- Do not move raw SkyLight callback delivery onto the main dispatch queue. Experiment 2116 made the official metric hang without parseable output instead of recovering callbacks.
- Do not keep broad raw SkyLight preflight benchmarking in the product path. Experiment 2117 recovered source delivery from the zero-frame state, but only to 95 HEVC complete groups with 6 drops and score 98.00.
- Do not repeat simple warm-up duration shrinkage as the next source recovery axis. Experiment 2118 showed a 10ms raw warm-up with 2 callbacks / 1 complete frame also restores delivery, but still regresses to 97 HEVC complete groups with 8 drops and score 94.00.
- Do not repeat the simple combination of 10ms raw SkyLight warm-up plus shared HEVC tile-session cadence. Experiment 2119 preserved the lower shared-session callback regime, but still reached only 96 complete groups with 6 drops and score 96.50.
- Do not keep the first-complete-frame SkyLight warm-up as implemented. Experiment 2120 numerically beat the old best at 108.00 and reduced drop pressure to 1 event, but the HEVC drop/stability regression still violates the product contract.
- Do not use first-callback SkyLight warm-up as a cleaner source unblock. Experiment 2121 still drained to 2 callbacks / 1 complete frame, kept 1 HEVC drop, and worsened group coherence to 95 complete / 2 incomplete groups.
- Keep first-clean-complete-frame SkyLight warm-up as the current best source-unblock primitive. Experiment 2122 reached 110.00 with 0 HEVC evaluator drops, but it still has only 95 complete groups and 2 incomplete groups.
- Do not move the runtime probe sample-window start to first complete tile group. Experiment 2123 regressed to 190 HEVC records, 94 complete groups, 5 drops, and 1 queued frame, so the 2122 group gap is not just a first-tile measurement-boundary artifact.
- Do not move raw warm-up stream stop ownership into the first clean callback path. Experiment 2127 reintroduced 1 HEVC drop, regressed to 94 complete groups and 5 incomplete groups, and still drained 2 warm-up callbacks for 1 complete frame.
- Do not fast-exit the first clean warm-up callback before dirty-rect/display-time bookkeeping. Experiment 2128 increased warm-up callbacks to 3, complete warm-up frames to 2, and HEVC drops to 30.
- Do not lower first-clean warm-up benchmark queue depth as a standalone cleanup. Experiment 2129 recovered 97 complete / 0 incomplete HEVC groups but introduced 31 HEVC drops.
- Do not combine lower first-clean warm-up queue depth with post-clean callback suppression. Experiment 2130 reduced warm-up callbacks to 1, but still produced 25 HEVC drops and regressed to 95 complete / 2 incomplete groups.
- Do not combine per-lane tile serial queues with post-clean warm-up suppression. Experiment 2131 drove encode queue wait near zero, but regressed to 94 complete groups and 2 HEVC drops.
- Do not poll `kVTCompressionPropertyKey_NumberOfPendingFrames` on the official encode path as admission/backpressure telemetry. Experiment 2132 sampled only every 8 encoded submissions and still regressed to 94 complete groups, 3 incomplete groups, and 4 HEVC drops.
- Do not release HEVC tiled source frames immediately after Metal staging as a group-throughput fix. Experiment 2133 tied the best HEVC shape at 95 complete groups with zero drops, but did not move the strict-group ceiling and reduced ProRes progression.
- Do not repeat raw shared-session reapplication without a new lever. Experiment 2134 shows the 2122 clean warm-up baseline can make shared HEVC tile VT session stable at 196 records, 98 complete groups, 0 incomplete groups, 0 drops, and ~36 ms callback latency, but it still only ties the best score because HEVC latency remains unscored and complete groups remain below 120.
- Do not combine post-clean warm-up callback suppression with shared-session topology. Experiment 2135 reduced warm-up callbacks to one, but shared-session HEVC regressed to 193 records, 96 complete groups, 1 incomplete group, and 1 drop.
- Do not overclock the shared-session VT target-frame-rate hint above tile-record cadence. Experiment 2136 changed the hint from 240 to 360, but max callback latency stayed about 37 ms and HEVC regressed to 96 complete groups with 5 drops.
- Do not force `EnableLowLatencyRateControl` on HEVC PQ shared tile sessions. Experiment 2137 passed unit tests but the official eval produced no parseable output after an extended wait and had to be killed.
- Do not optimize host probe drain cadence. Faster drain destabilized measurement and did not reveal hidden encoder headroom.
- Be careful with detailed source diagnostics: forcing cadence/timing trackers on the hot path reduced source counts during measurement, so use them as diagnostic-only evidence, not a performance baseline.

## Failure Retrospective Protocol

Every discarded experiment has to answer "why did it fail?" before the next commit. The minimum review is:

| Question | Required evidence |
| --- | --- |
| Which boundary broke first? | Classify the first regression as source ingress, pending admission, Metal stage, VT submit/admission, VT output callback, Lumen forwarding, HDR validation, drop/stability accounting, or build/test failure. |
| Did it improve the intended stage? | Compare the targeted counters against the current baseline: HEVC frames, staged VT submissions, source count if available, Metal stage, VT encode call, VT callback latency, startup, queued/dropped frames, and drop events. |
| Did it preserve the contract? | Confirm HEVC Main10/PQ HDR, partial HDR requirement, target resolution, target fps, low latency, quality/bitrate policy, and ProRes suite health when applicable. |
| Why is it closed or still open? | State whether the axis is closed, invalid/inconclusive, or worth retrying only with a changed topology. |
| What should the next experiment avoid? | Record the concrete anti-repeat lesson in `.codex-autoresearch/experiment_notes.md`, not just in chat. |

Stability failures are usually not crashes in this campaign. If `DROP_EVENTS` is nonzero, trace the event source before interpreting the score. Current evidence points to MacDisplayKit source-frame drops when upstream admission gets ahead of VT submit/output and the pending window saturates; those drops enter the evaluator as stability penalties even when HDR and forwarding remain valid.

## Highest Priority Bottleneck

The best current explanation has shifted again:

1. For HEVC, single full-frame VT output was the old ceiling; the kept tile-record protocol now reaches 194 HDR tile records with zero drops.
2. For ProRes, experiment 2061-2063 recovers source cadence and reaches 134-141 HDR records with zero drops, so ProRes frame count is no longer the primary limiter.
3. The remaining HEVC gap is now 97 complete logical tile groups versus the 120 target. Tile geometry matters: column partitioning improved the previous 89-group audit.
4. The latest tail-latency evidence points at Metal conversion plus VT encode-call/output drain. HEVC VT encode-call P95 is about 21 ms in the diagnostic run, so the next useful structural experiment should reduce per-lane Metal/VT work or avoid requiring both lanes to complete each logical frame inside the same 8.33 ms cadence.
5. The first topology-contract probe supports the latter path: independent tile freshness can exceed 120 updates in the current stream, while strict same-group completion cannot. The next product-level bottleneck is the receiver contract: tile manifest, latest-tile composition, skew bounds, and visual-coherence rules.
6. The skew-bounded probes refine this: the current freshness stream needs a compositor policy, not just a metric rename. A plausible next contract is to accept latest-tile updates only under a one-frame or two-frame skew budget and explicitly account for held/dropped over-skew tile updates.
7. The skew distribution is lane-asymmetric, but 2076 shows that the stale lane follows enqueue order. That makes the next structural question narrower: strict groups are capped by ordered tile output cadence, while freshness composites need an order-aware bounded-staleness contract.
8. Sender-side paired output is the wrong layer for that contract. It improves skew distribution but causes drop amplification, so the next experiment should model receiver-side arbitration without blocking encoded tile output.
9. The first receiver-side model closes the naive latest-pending policy: two-frame arbitration is far below 120 updates, while three-frame arbitration jumps to 190 updates. The useful path is now phase-aware receiver scheduling or cadence normalization that can recover two-frame coherence without sender hot-path buffering.
10. The phase-normalized queue probe shows that two-frame coherence is present in the stream, but heavy in-probe arbitration destabilizes HEVC. This points toward offline trace analysis, lightweight metadata collection, or actual receiver-side implementation rather than more probe-side queue simulation.
11. The offline FIFO-front probe shows that phase reconstruction cannot just preserve queues and consume their fronts. The next useful measurement should expose tile arrival phase directly, or choose nearest-in-window candidates against a reconstructed presentation clock.
12. Source timestamp pairing also fails: nearest source-display-time matches equal strict groups. Receiver timebase reconstruction needs receiver-side arrival/callback phase, not only capture/display timestamps.
13. Arrival timestamp pairing also fails: nearest probe-drain arrival matches equal strict groups. The 2081 two-frame recovery came from stateful partial presentation updates, not from discovering more whole-tile pairs.
14. Bounded newest-pending presentation also fails. It drops the history that produced the 2081 signal, so queue-depth limiting by itself is not the stateful presentation contract.
15. Returning to Metal conversion with a fused no-scale 420 kernel also fails. The same staged BGRA topology still regresses to 94 complete groups and nonzero drops, so the next encoder-side experiment needs a different pacing/ownership topology rather than only fewer shader dispatches.
16. Moving color conversion into VT by submitting staged BGRA to a Main10 HEVC session fails harder. The explicit staged 420v10 path remains the better baseline; future VT/output work should not assume VT internal conversion is cheaper.
17. Staggering tile-lane PTS phases also fails. VT/output work should not assume a simple timestamp cadence offset can desynchronize hardware drain; the next encoder-side branch needs different session ownership, callback isolation, or a real client-side tile presentation contract.
18. Deferring callback-local HDR augmentation also fails. The VT callback latency bucket reflects downstream encoder/output drain more than local metadata wrapping work, so callback work placement alone is not enough.
19. Explicit per-frame duration also fails. The VT cadence problem is not fixed by making frame duration more concrete; this appears to add ordering or scheduler pressure rather than reducing drain jitter.
20. Sharing the tile-lane Metal command queue also fails. GPU completion ownership at the command-queue level does not explain the strict-group plateau; the bottleneck is deeper than simple Metal queue burst regularity.
21. Sharing the tile-lane VT output queue also fails. Output-handler serialization after VT callbacks does not change the ~70 ms callback latency regime or the 96 strict-group plateau, so the remaining encoder-side branch must alter VT session/drain ownership itself rather than only downstream queue ownership.
22. Skipping explicit VT session preparation also fails. The callback regime and strict-group plateau survive lazy session start, so the plateau is not just a `PrepareToEncodeFrames` lifecycle artifact.
23. Sharing the tile-lane VT session changes the callback regime but not the logical-group ceiling. One session reduced callback latency from ~70 ms to ~36 ms and doubled VT submissions, but strict complete groups stayed 96 and drops increased. The next useful session-topology branch should control admission/pacing around this lower-latency regime rather than treating raw shared-session throughput as a solution.
24. Matching shared-session hints to tile-record cadence preserves the lower-latency regime and recovers 97 complete groups, but drops remain. This keeps the shared-session branch alive only if the next experiment adds explicit in-flight/admission control.
25. A raw staging-slot cap is the wrong admission-control primitive. It keeps the lower callback-latency regime and removes drops, but turns pressure into `stagingSlotUnavailable` failures and collapses useful groups to 83.
26. Synchronous soft pacing is also the wrong primitive. Waiting inside the capture/encode path destabilizes ingress and can fail the HEVC runtime probe outright, so any remaining shared-session admission work must be non-blocking or moved out of the capture hot path.
27. Shared-session timestamp grouping is closed. Giving both tiles from one source frame the same PTS produces no useful HEVC output, so VT must see tile records as independently timestamped submissions unless a deeper encoded-tile protocol changes.
28. Shared-session non-blocking staged drain is also closed as a generic encoder-layer primitive. It preserves the lower callback-latency regime but does not improve strict group completion and breaks ProRes proxy, so remaining shared-session work must be codec-scoped or moved into an explicit receiver/client tile presentation contract.
29. Source-level pending-window reduction is also closed for shared-session admission. Lowering the HEVC tiled SkyLight pending window to 12 destabilizes the runtime probe, so the source ingress window should not be used as the next pacing knob.
30. Source-level pending-window expansion is closed too. Raising the HEVC tiled SkyLight pending window to 24 also destabilizes the runtime probe, so the shared-session branch should not continue with source-window sizing in either direction.
31. Shared-session zero frame-delay is closed. Forcing `MaxFrameDelayCount=0` on the shared HEVC tile processor fails both HEVC and ProRes runtime probes, so simple VT frame-delay knobs do not provide safe admission control around the lower-latency shared-session regime.
32. Probe hot-path drop-source classification is closed as implemented. Adding dropped-frame message buckets to the official runtime probe produced zero HEVC/ProRes frames, so future attribution should parse existing logs or use a separate non-scoring diagnostic runner.
33. The current clean measurement path can enter a no-frame startup failure before VT submission. Experiment 2104 converted that state into an explicit probe error and confirmed diagnostics stop at SkyLight/raw-private setup with 0 VT submissions. Future work should restore valid source delivery before further encoder or receiver tuning.
34. The no-frame source startup failure survives a probe-level capture restart. Experiment 2105 showed one stop/start retry still yields 0 HEVC/ProRes frames, startup -1.0 ms, and 0 VT submissions, so the next startup/source-delivery work must instrument or isolate the source callback/forwarding boundary rather than retrying the probe start sequence.
35. The current HEVC no-frame startup state is upstream of Lumen core-forwarding. Experiment 2106 exposed existing capture-session counters and showed `captureSessionIsRunning=true`, `captureSessionEmittedFrameCount=0`, `captureSessionProcessingFailureCount=0`, `captureSessionLastError=n/a`, and `VT_SUBMITTED_FRAME_COUNT=0`; the next boundary is raw-private SkyLight source callback emission or source-to-processor handoff.
36. The current no-frame startup state is upstream of the source-to-processor handoff. Experiment 2107 showed `SKYLIGHT_SOURCE_CALLBACK_COUNT=0` and `SKYLIGHT_SOURCE_TIMER_TICK_COUNT` around 1200 for both HEVC and ProRes, so the raw-private SkyLight display stream starts enough for the runtime timer to run but never invokes the frame callback during the metric window.
37. Private direct IOSurface fallback does not recover the current official probe. Experiment 2108 forced the probe away from raw SkyLight and both codec probes failed immediately with a nonzero private-direct warmup status, so the next startup experiment should stay on raw SkyLight callback creation/start parameters or introduce a different fallback backend.
38. Raw SkyLight rect geometry properties do not recover callback delivery. Experiment 2109 added source rect, destination rect, and preserveAspectRatio properties only in the probe, but both codec probes still produced 0 frames and 0 VT submissions, so the missing callback is not explained by absent CGDisplayStream-style rect keys.
39. Nil raw SkyLight stream properties do not recover callback delivery either. Experiment 2110 removed all stream properties only in the probe and still failed the official metric, including HEVC runtime failure and ProRes 0 frames/0 VT submissions, so the next property-profile work should not jump to nil-profile extremes.
40. The existing private probe's rectless-minimal property shape does not recover callback delivery. Experiment 2111 set `showCursor=false`, `minimumFrameTime=0`, `queueDepth=3`, `preserveAspectRatio=false`, and no rects, but the official runtime probe still failed with no ProRes frames or VT submissions.
41. Strong callback-owner retention does not recover raw SkyLight callback delivery. Experiment 2112 removed the weak-owner check from the raw stream callback path, but both codec probes still produced 0 frames and 0 VT submissions, so callback block owner lifetime is not the first failing boundary.
42. A generic public CGDisplayStream encoded-source fallback does not recover the official probe. Experiment 2113 wrapped `MDKCaptureSessionFactory(.cgDisplayStream)` as an encoded source and forced it from the probe, but both HEVC and ProRes failed before producing usable frame diagnostics.
43. Starting a raw-created stream through public `CGDisplayStreamStart` does not recover callback delivery. Experiment 2114 kept `SLDisplayStreamCreateWithDispatchQueue` but replaced `SLDisplayStreamStart`, and HEVC still produced 0 frames/0 VT submissions while ProRes failed the runtime probe.
44. Raw SkyLight create/start lifecycle is not the failing boundary. Experiment 2115 showed `SLDisplayStreamCreateWithDispatchQueue` and `SLDisplayStreamStart` are present, stream creation succeeds, `SLDisplayStreamStart` returns 0, size/pixel format/properties match the request, and HEVC still produces 0 frames and 0 VT submissions.
45. Main-queue raw SkyLight callback targeting is not viable. Experiment 2116 forced the raw stream to use `dispatch_get_main_queue()` only in the probe, but the official metric hung without parseable output rather than delivering source frames.
46. Raw SkyLight preflight benchmarking changes the operating state but is not a keepable fix. Experiment 2117 ran a short standalone raw benchmark before encoded capture; it restored HEVC/ProRes frame delivery but only reached 95 HEVC complete groups with 6 drops and 98.00 total score.
47. Minimal raw SkyLight warm-up is enough to unblock startup but not enough to improve the target. Experiment 2118 reduced preflight to 10ms and surfaced 2 callbacks / 1 complete warm-up frame, restoring delivery but landing at 97 HEVC complete groups with 8 drops and 94.00 total score.
48. Combining the minimal raw warm-up with shared HEVC tile-session cadence preserves the lower callback-latency regime but still fails the product contract. Experiment 2119 reported HEVC callback latency around 35.7 ms and VT encode-call average around 5.1 ms, but only 96 complete groups, 1 incomplete group, 6 drops, and 96.50 total score; lower callback latency alone does not lift strict groups once source delivery is unblocked.
49. Event-driven first-complete-frame warm-up is a partial source-unblock breakthrough but not a keepable result. Experiment 2120 reached 108.00 total score with 194 HEVC tile records, 97 complete groups, 0 incomplete groups, and only 1 HEVC drop event, but the drop/stability penalty still violates the no-regression contract. The useful signal is that source warm-up perturbation can be reduced substantially without changing codec quality or VT topology.
50. First-callback warm-up is weaker than first-complete-frame warm-up. Experiment 2121 still produced 2 callbacks / 1 complete warm-up frame, kept the same 1 HEVC drop event, and regressed HEVC coherence to 95 complete groups with 2 incomplete groups, so the unblock likely needs a complete raw frame or at least cannot be cleaned by stopping on any callback.
51. First-clean-complete-frame warm-up is the current kept best. Experiment 2122 reached 110.00 with 0 HEVC evaluator drops and 140 ProRes HDR frames, proving the final drop in 2120 can be removed by waiting for a complete raw warm-up frame with update drop count 0. The remaining HEVC issue is no longer evaluator drop stability but strict group coherence: 95 complete groups and 2 incomplete groups.
52. Probe sample-window priming is not a safe way to recover missing groups. Experiment 2123 waited for the first complete tile group before opening the 1s sample window, but HEVC fell to 190 records, 94 complete groups, 2 incomplete groups, 5 drops, and 1 queued frame. That closes the idea that 2122's incomplete groups are primarily a receiver boundary artifact in the current official probe.
53. Lightweight offline stateful presentation modeling does not recover a two-frame product contract. Experiment 2124 added post-capture stateful tile-presentation counters and reported 12 one-frame, 116 two-frame, and 191 three-frame updates, while HEVC still had only 96 complete groups and 3 drop events. This confirms the stream is mostly salvageable only at a three-frame budget under this front-hold model, and closes this model as a replacement for the heavier 2081 online stateful arbitration.
54. Callback-gated source release is not the missing shared-session backpressure primitive. Experiment 2125 re-applied the shared tile VT session and delayed tile source release until VT output callback completion; callback latency stayed low at about 36 ms, but HEVC still had only 96 complete groups and drop events rose to 13. This closes callback-token source release as a safe non-blocking admission-control strategy.
55. Post-clean raw warm-up callback suppression is a useful but insufficient startup cleanup. Experiment 2126 reduced warm-up callbacks from 2 to 1, kept 0 HEVC drops, and nudged HEVC complete groups from the 2122 baseline's 95 to 96, but it only tied the 110.00 score and still missed 120 groups. Do not repeat the exact accounting suppression patch; a future warm-up variant must change actual callback/stop perturbation or combine with another non-shared-session group-coherence improvement.
56. Callback-side async stop is worse than caller-side clean-frame warm-up stop. Experiment 2127 moved `SLDisplayStreamStop` onto a callback-triggered stop queue, but HEVC regressed to 94 complete groups, 5 incomplete groups, 1 drop event, and 2 warm-up callbacks for 1 complete frame. This closes stop-ownership relocation as the warm-up cleanup path.
57. First-clean callback fast-exit is unsafe. Experiment 2128 skipped reduced-dirty-rect/display-time bookkeeping after the first clean warm-up callback, but HEVC collapsed to 30 drop events and warm-up callbacks increased to 3 / 2 complete frames. The first callback's normal bookkeeping appears to be part of the safe warm-up lifecycle, not removable overhead.
58. Lower warm-up queue depth is not a free group-coherence fix. Experiment 2129 changed only the preflight clean warm-up benchmark to `queueDepth=1`, and it reached 194 HEVC records with 97 complete / 0 incomplete groups, but at 31 HEVC drops. This shows the strict group shape can move via warm-up source pressure, but the current mechanism violates the stability contract too severely to keep.
59. The 2126 one-callback cleanup does not rescue lower warm-up queue depth. Experiment 2130 combined `queueDepth=1` warm-up with post-clean callback suppression and got one warm-up callback, but HEVC still had 25 drops and fell back to 95 complete / 2 incomplete groups. The lower-queue-depth drop pressure is not merely post-clean callback accounting.
60. Per-lane serial queue scheduling remains closed even with one-callback warm-up cleanup. Experiment 2131 reduced encode queue wait to almost zero, but HEVC regressed to 190 records, 94 complete groups, and 2 drops. The missing groups are not caused by source-callback lane enqueue serialization.
61. VT pending-depth polling is not a safe admission signal on the official path. Experiment 2132 sampled `NumberOfPendingFrames` only every 8 encoded submissions, yet HEVC regressed to 191 records, 94 complete groups, 3 incomplete groups, and 4 drops. Future backpressure work should not depend on synchronous VT property reads in the encode hot path.
62. HEVC tiled post-Metal source release is not the missing permit boundary. Experiment 2133 released the original source frame after Metal staging instead of after VT submit and still produced the 2122 HEVC shape: 192 records, 95 complete groups, 2 incomplete groups, and 0 drops. Source IOSurface retention between Metal completion and VT submit is not the group-coherence limiter.
63. Shared HEVC tile VT session is still a live topology branch when paired with the clean warm-up baseline. Experiment 2134 re-applied the 2095 shared-session tile-record cadence on top of 2122 and reached 196 records, 98 complete groups, 0 incomplete groups, 0 drops, callback latency average 35.947 ms, and VT encode-call average 5.215 ms. It ties rather than beats the 110.00 best because HEVC latency still scores 0 and complete groups remain below 120, but this is the strongest evidence that session topology changes the operating regime without necessarily violating stability.
64. Post-clean warm-up callback suppression should not be layered onto the shared-session regime. Experiment 2135 kept the lower callback latency but regressed HEVC to 193 records, 96 complete groups, 1 incomplete group, and 1 drop despite reducing warm-up callbacks to one. The 2134 shared-session stability appears to depend on preserving the full 2122 clean warm-up lifecycle, not suppressing queued callback accounting.
65. Shared-session target-frame-rate overclocking is not the missing latency lever. Experiment 2136 raised the shared session hint to 360 fps, but max callback latency stayed around 37.077 ms, HEVC latency still scored 0, and the run regressed to 192 records, 96 complete groups, and 5 drops. Higher expected-frame-rate pressure destabilizes cadence rather than lowering the callback tail.
66. VideoToolbox low-latency rate-control override is not a safe shared-session latency lever. Experiment 2137 forced LLRC only for the shared HEVC tile processor, but the official eval hung without parseable output and had to be killed. The remaining latency gate should not be attacked by enabling LLRC for HEVC PQ in the production probe.
67. Alternating shared-session tile submit order is not a safe way to reduce ordered-output skew. Experiment 2138 lowered average callback latency and encode-call time, but only because the shared session delivered far fewer tile records: 157 records, 78 complete groups, and 158 VT submissions. This closes simple submit-order balancing inside the shared-session branch.
68. Encoded tile metadata is sufficient for substream identification, but not sufficient by itself as a product fix. Experiment 2139 counted 159 contract-ready tile updates with 0 invalid records, proving the manifest ingredients are present after priming. The missing piece is the actual receiver/client presentation semantics that consume those updates, not more metadata-only probe accounting.
69. Low-overhead post-capture replay of the 2081 phase queue is not a two-frame solution. Experiment 2140 avoided the hot-path drop pressure and had no trace overflow, but it recovered only 8 two-frame updates and 158 three-frame updates. The next client contract must either explicitly accept the three-frame shape or validate a real compositor/display behavior that the source-time replay model cannot simulate.
70. Encoded-tile capability inference is not a scoring or stability fix. Experiment 2143 inferred encoded tile stream support from the already-required HDR tile overlay plus per-frame HDR metadata capabilities when RTSP/HTTP clients omitted the newer explicit flag, but the official probe regressed to 12 HEVC drop events with 193 records and 96 complete groups. Keep encoded-tile support explicit until the real receiver/client presentation contract is implemented and validated.
71. Shared-session one-turn secondary-tile deferral is safe but insufficient. Experiment 2144 preserved 0 drops, lowered callback latency to the shared-session regime, and produced 97 complete groups, but it did not beat the prior 2134 shared-session 98-group shape. The remaining shared-session bottleneck is not just bursty same-stack admission of the second tile.
72. Tile-count-only per-tile update metadata is insufficient. Experiment 2145 set `tileCount=1` while preserving `encodedLaneCount=2`, but complete groups stayed 97 because `frameGroupID=0` still made the probe group both tiles by source sequence. A true per-tile update contract must alter group identity, not just expected tile count.
73. Per-tile group identity crosses the 120 complete-group line in the official probe. Experiment 2146 produced 194 complete groups and 0 HEVC drops by assigning independent group IDs to tile updates. This is strong evidence that the main remaining barrier is the encoded-tile presentation contract, not raw encoder throughput, but the change should be promoted only with an explicit client/compositor contract because it redefines completeness semantics.

The target is now to make the HEVC tile-stream contract rigorous enough for the product: either clients and probes must explicitly consume independent encoded tile records as tile substreams, or the encoder topology must deliver 120 complete logical tile groups without treating valid substreams as drops.

## Next Structural Directions

- Tile semantics branch: the per-record metadata ingredients are present after priming, but a probe-only counter is closed. Next work must add or validate the actual receiver/client presentation contract that consumes those tile records, not merely count them.
- Tile freshness branch: define a skew-bounded partial-freshness contract where clients can update a tile as soon as its substream arrives after all tiles are primed, instead of waiting for all lanes from the same frame group. The first version should expose tile age/skew metadata and hold/drop over-budget updates rather than blindly presenting every latest tile.
- Ordered-output branch: receiver-side temporal arbitration is now preferred over sender-side paired emission, but the naive latest-pending budget, heavy probe-side queues, FIFO-front offline pairing, fixed-trace source-time phase replay, source-time nearest matching, arrival-time nearest matching, bounded newest-pending state, first-complete-group probe priming, lightweight front-hold stateful modeling, and shared-session submit-order alternation are closed. The next useful version should move to an actual client-side presentation contract instead of more probe-only pairing searches.
- Logical-group throughput branch: if the product requires complete frames before delivery, redesign HEVC lane scheduling so both tile lanes produce at least 120 complete groups/s; do not reintroduce complete-group gating on the current queue because prior gating collapsed throughput.
- Client contract branch: define whether Android/general clients consume independent HEVC substreams directly, reassemble them, or require a logical-frame manifest beside each tile record.
- Negotiation branch: encoded-tile-stream support should remain explicit for now. Inferring it from HDR tile overlay plus per-frame HDR metadata is plausible for product compatibility, but experiment 2143 did not improve the official path and violated the no-drop stability contract.
- Lane-count branch is mostly closed for direct scaling: 3 lanes increased record count but made logical groups worse. Prefer 2-lane scheduling/region work before any higher lane count.
- Lane-order branch is closed for simple rotation and shared-session alternation: reordering existing lane work caused incomplete groups or severe tile-record collapse. Per-lane queue isolation also tied the current 97-group best without moving VT submission/output, so prefer reducing per-lane work or changing the encoded tile contract rather than rescheduling the same work.
- Tile-width alignment branch is closed for simple asymmetric CTU rounding: equal half-width columns remain the best measured geometry.
- ProRes catch-up branch is kept and should not be widened unless new evidence shows drop-free ProRes overproduction hurts battery or latency.
- HEVC admission branch is lower priority now: keep the 2058 tile path unless a new change regresses HDR, drops, or Android-required HEVC Main10 support.
- Source-release branch: current tiled HEVC post-submit release is not the group bottleneck. Experiment 2133 shows post-Metal source release ties HEVC but does not improve complete groups and hurts ProRes progression.
- VT cadence branch: simple timestamp phase staggering is closed. If this branch continues, change VT session topology or callback/output ownership rather than only shifting presentation timestamps.
- Callback ownership branch: simple deferral of HEVC HDR augmentation and simple tile-lane output-queue sharing are closed. Future callback work must change VT session/drain ownership more structurally than moving sample-buffer wrapping or output delivery onto a different serial queue.
- VT timing-hint branch: explicit per-frame duration is closed. Keep session-level duration hints and avoid more timestamp/duration-only tweaks unless coupled to a different session topology.
- VT lifecycle branch: skipping `VTCompressionSessionPrepareToEncodeFrames` is closed. Do not retry lazy session start without changing session ownership or output contract.
- VT session topology branch: one shared tile VT session is still not keepable as-is, but it is no longer closed as merely unstable. Tile-record cadence hints alone were insufficient before 2122, hard staging-slot caps create admission failures, synchronous pending waits destabilize capture, same-PTS grouping kills output, generic non-blocking staged-submission drain breaks ProRes while regressing strict groups, source pending-window sizing fails in both directions, `MaxFrameDelayCount=0` fails both runtime probes, callback-gated tile source release worsens drops, post-clean warm-up callback suppression regresses the shared-session stability shape, overclocking the target-frame-rate hint to 360 worsens drops without reducing callback max latency, and forcing HEVC PQ low-latency rate control hangs the official eval. Experiment 2134 shows the clean warm-up baseline makes raw shared-session stable at 98 complete groups and 0 drops with ~36 ms callback latency, so any further shared-session work must add a genuinely new score-moving lever while preserving the 2122 warm-up lifecycle and 240 fps tile-record cadence hint.
- VT pending-depth branch: synchronous `NumberOfPendingFrames` polling is closed for the official path. If pending depth is needed for diagnosis, gather it outside the scoring hot path or from a separate targeted run.
- Metal ownership branch: shared tile-lane command queue is closed. Do not repeat queue-sharing without changing VT session ownership or presentation contract.
- Probe instrumentation branch: do not add new counters directly to the official runtime probe unless the measurement target is otherwise unchanged and a baseline sanity run confirms frame delivery. Prefer log-parser-only diagnostics for attribution work.
- Startup/source-delivery branch: the immediate blocker is now a zero-frame startup path on the clean tree. Before more VT/session/presentation tuning, isolate why SkyLight/raw-private capture starts but no forwarded frames reach VT in the official probe. Experiment 2105 closes a generic stop/start retry, experiment 2106 closes Lumen core-forwarding as the first explanation for HEVC zero frames, experiment 2107 closes source-to-processor handoff as the next boundary because the raw SkyLight callback count is zero while the runtime timer ticks, experiment 2108 closes private direct IOSurface fallback in the current probe, experiment 2109 closes rect/preserveAspectRatio property additions, experiment 2110 closes nil stream properties, experiment 2111 closes the private-probe rectless-minimal profile, experiment 2112 closes weak callback-owner retention, experiment 2113 closes a generic public CGDisplayStream encoded-source wrapper, experiment 2114 closes public `CGDisplayStreamStart` as a raw-stream startup substitute, experiment 2115 closes raw create/start availability/status diagnostics, and experiment 2116 closes main-queue callback delivery. Experiment 2117 shows a standalone raw benchmark before encoded capture can restore delivery, and experiment 2118 shows the unblock can shrink to a 10ms warm-up with 2 callbacks / 1 complete frame. Experiment 2119 closes the simple combination of that minimal unblock with shared HEVC tile-session cadence: lower callback latency survives, but strict groups and drops do not improve enough. Experiment 2120 shows event-driven first-complete-frame warm-up is a strong source-unblock variant but still has one HEVC drop. Experiment 2121 shows first-callback warm-up is not cleaner and worsens group coherence. Experiment 2122 keeps a first-clean-complete-frame warm-up as the current best, eliminating HEVC evaluator drops. Experiment 2124 shows official-probe presentation bookkeeping can still perturb drops. Experiment 2126 shows one-callback warm-up cleanup can improve strict groups by one without drops, but is insufficient alone. Experiment 2127 closes callback-side async stop ownership because it reintroduces drops and worsens groups. Experiment 2128 closes first-clean callback fast-exit because it catastrophically increases HEVC drops. Experiment 2129 closes standalone lower warm-up queue depth because it restores 97/0 groups only by introducing catastrophic drops. Experiment 2130 closes lower warm-up queue depth plus post-clean suppression because it still drops heavily and loses the 97/0 group shape. Experiment 2131 closes per-lane serial queues plus post-clean suppression because queue-wait removal still worsens groups and drops. The next useful work should preserve the 2122 source-unblock primitive and target downstream VT/output cadence or client presentation contract, not source warm-up pressure or lane enqueue serialization.

## Measurement Command

```bash
python3 tools/autoresearch/eval_host_stream.py --workspace src/platform/macos/Lumen.xcworkspace --scheme LumenTuistTests --log "/Users/skyline23/Library/Application Support/Lumen/lumen.log" --target-width 3512 --target-height 2290 --target-fps 120 --codec-suite hevc,prores-proxy --require-partial-hdr --require-low-latency --battery-policy adaptive-hdr
```
